# نشر تطبيق INTERSTELLAR CLUB على السحابة

هذا الدليل يوضح كيفية نشر تطبيق INTERSTELLAR CLUB على منصة Heroku ليكون متاحاً 24/7 دون الحاجة لتشغيل الحاسوب المحلي.

## المتطلبات الأساسية

1. حساب GitHub
2. حساب Heroku (مجاني)
3. Git مثبت على الحاسوب

## خطوة 1: رفع المشروع إلى GitHub

### أ. إنشاء repository جديد على GitHub
1. اذهب إلى [GitHub.com](https://github.com) وقم بتسجيل الدخول
2. اضغط على "+" في الأعلى ثم "New repository"
3. أدخل اسم المشروع: `interstellar-club`
4. اضغط "Create repository"

### ب. رفع الملفات إلى GitHub
```bash
# في مجلد المشروع
git init
git add .
git commit -m "Initial commit - INTERSTELLAR CLUB application"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/interstellar-club.git
git push -u origin main
```

## خطوة 2: إنشاء تطبيق Heroku

### أ. التسجيل في Heroku
1. اذهب إلى [heroku.com](https://heroku.com) وقم بإنشاء حساب
2. قم بتأكيد البريد الإلكتروني

### ب. إنشاء التطبيق
1. اضغط "New" → "Create new app"
2. أدخل اسم التطبيق (مثل: `interstellar-club-university`)
3. اختر المنطقة الأقرب إليك
4. اضغط "Create app"

### ج. ربط GitHub بالتطبيق
1. في لوحة التحكم للتطبيق، اذهب إلى تبويب "Deploy"
2. في قسم "Deployment method"، اختر "GitHub"
3. أدخل اسم repository: `interstellar-club`
4. اضغط "Search" ثم "Connect"

### د. تفعيل النشر التلقائي
1. فعل "Enable Automatic Deploys" من الفرع `main`
2. اضغط "Deploy Branch" للنشر الأولي

## خطوة 3: إعداد متغيرات البيئة

### أ. إعداد البريد الإلكتروني
1. في لوحة تحكم Heroku، اذهب إلى "Settings" → "Config Vars"
2. أضف المتغيرات التالية:

```
EMAIL_HOST = smtp.gmail.com
EMAIL_PORT = 587
EMAIL_USER = interstellarclub99@gmail.com
EMAIL_PASS = miofeskjjleuwjam
EMAIL_FROM = interstellarclub99@gmail.com
BASE_URL = https://interstellar-club-university.herokuapp.com
```

**ملاحظة مهمة:** تأكد من أن بريدك الإلكتروني يدعم إرسال الرسائل عبر SMTP.

## خطوة 4: النشر والتشغيل

### أ. النشر الأولي
بعد ربط GitHub، اضغط "Deploy Branch" في قسم "Manual deploy"

### ب. مراقبة النشر
- اذهب إلى تبويب "Activity" لمراقبة عملية النشر
- عند نجاح النشر، ستجد رابط التطبيق في الأعلى

## خطوة 5: التحقق من العمل

1. اضغط على رابط التطبيق في Heroku
2. يجب أن يفتح موقع INTERSTELLAR CLUB
3. جرب إنشاء حساب جديد وتسجيل الدخول

## استكشاف الأخطاء

### مشاكل شائعة:

#### 1. خطأ في قاعدة البيانات
- Heroku يستخدم نظام ملفات مؤقت، لذا SQLite قد يفقد البيانات عند إعادة التشغيل
- الحل: استخدام PostgreSQL في المستقبل

#### 2. خطأ في البريد الإلكتروني
- تأكد من إعدادات Gmail App Password
- تأكد من تفعيل "Less secure app access" أو استخدام App Password

#### 3. خطأ في النشر
- تحقق من logs في تبويب "More" → "View logs"
- تأكد من أن جميع الملفات موجودة في GitHub

## الصيانة والتحديث

### تحديث التطبيق:
```bash
# في مجلد المشروع المحلي
git add .
git commit -m "تحديث جديد"
git push origin main
```
Heroku سيقوم بالنشر التلقائي

### مراقبة الاستخدام:
- في لوحة Heroku، اذهب إلى "Resources" لمراقبة الاستخدام
- الخطة المجانية محدودة بـ 550 ساعة شهرياً

## ملاحظات مهمة

1. **النسخ الاحتياطي**: احرص على عمل نسخ احتياطية من قاعدة البيانات بانتظام
2. **الأمان**: لا تشارك مفاتيح API أو كلمات المرور
3. **التكلفة**: الخطة المجانية محدودة، قم بالترقية عند الحاجة
4. **الدعم**: استخدم مجتمع Heroku أو الدعم الفني للمساعدة

## الخطوات التالية (اختيارية)

### ترقية قاعدة البيانات إلى PostgreSQL:
1. في Heroku، أضف add-on "Heroku Postgres"
2. حدث `DATABASE_URL` في متغيرات البيئة
3. حدث الكود لاستخدام PostgreSQL بدلاً من SQLite

### إضافة دومين مخصص:
1. في "Settings" → "Domains"
2. أضف دومينك المخصص
3. حدث DNS settings لدى مزود الدومين

---

**تهانينا!** موقع INTERSTELLAR CLUB الآن متاح على الإنترنت 24/7 🎉
